services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MONGO_URL=mongodb://db:27017
    depends_on:
      - db
  db:
    image: mongo:latest
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: